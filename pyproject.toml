[project]
name = "triagem-laudos"
version = "0.1.0"
description = "Classificação automática de condições médicas em laudos (Tech Challenge Fase 03)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.6",
    "scikit-learn>=1.4",
    "pandas>=2.2",
    "joblib>=1.3",
    "prometheus-client>=0.20",
    "onnxruntime>=1.20",
    "skl2onnx>=1.17",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.8",
    "httpx>=0.27",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 88
target-version = "py312"
src = ["src", "tests", "scripts", "dags"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
